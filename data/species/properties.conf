#$dir = /home/user/linnaeus/species
$dir = data/species

variantMatcher=$dir/dict-species.tsv
 
ppStopTerms=$dir/stoplist.tsv
ppAcrProbs=$dir/synonyms-acronyms.tsv
ppSpeciesFreqs=$dir/species-frequency.tsv

ignoreCase=true

postProcessing
